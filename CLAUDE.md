# CLAUDE.md

Bu dosya, bu repo üzerinde çalışan Claude Code için proje bağlamını tanımlar. Detaylı gerekçelendirme ve tam plan için `akilli-destek-asistani-proje-raporu.md` dosyasına bakılabilir; bu dosya onun özetlenmiş, kod-odaklı halidir.

## Proje Özeti

"Akıllı Müşteri Destek Asistanı" — gelen müşteri taleplerini (e-posta/form) otomatik sınıflandıran, şirketin kendi bilgi tabanından (SSS/dokümantasyon) ilgili içeriği bulup (RAG) buna dayanan bir yanıt taslağı üreten ve bu taslağı bir temsilcinin onayına sunan bir sistem. Sistem **hiçbir zaman otomatik olarak müşteriye yanıt göndermez** — her yanıt bir insan tarafından onaylanmalı, düzenlenebilir veya reddedilebilir olmalıdır. Bu, projenin en temel ve değiştirilemez tasarım kuralıdır.

## Mevcut Aşama

**Hafta 4 tamamlandı (eval seti hariç) + Hafta 5'in auth, çoklu şirket ve temel analitik kısımları bitti + Hafta 6'nın ilk iki fikri (lead qualification, tool-use) uygulandı. Eval seti ve gerçek e-posta entegrasyonu kullanıcı kararıyla şimdilik bilinçli olarak bırakıldı (aşağıya bakılmalı) — mevcut haliyle proje herkese açık form + müşteri portalı üzerinden uçtan uca çalışır durumda.**

Tamamlananlar:
- Hafta 1: uçtan uca "veri gösterme" akışı çalışıyor (FastAPI + Next.js + pgvector'lı Postgres). `tickets` tablosu, gerçek veriyle dolu (Kaggle "Customer Support on Twitter" dataset'inden 300 gerçek müşteri talebi — `scripts/ingest_kaggle_tickets.py`).
- Hafta 2: `knowledge_base_chunks` tablosu (pgvector aktif), Bitext dataset'inden 27 intent için SSS içeriği (`scripts/ingest_kb.py`), Gemini embedding entegrasyonu (`backend/app/services/embeddings.py`, `gemini-embedding-001`, 768 boyut), retrieval fonksiyonu (`backend/app/services/retrieval.py`, pgvector kosinüs mesafesi).
- Hafta 3: `call_llm(prompt, context)` sarmalayıcısı (`backend/app/services/llm.py`, `gemini-2.5-flash`, geçici 503 hatalarında otomatik yeniden deneme). Sınıflandırma servisi (`classification.py`, 11 sabit kategori). Taslak üretim servisi (`draft_generation.py`) — retrieval'dan bulduğu SSS içeriğine dayanarak Türkçe taslak yazıyor, SSS'te karşılığı yoksa bunu taslakta açıkça belirtiyor. Prompt'lar `backend/app/services/prompts/*.txt` içinde, koda gömülü değil.
- `draft_responses` tablosu (`retrieved_context` JSON olarak SSS kaynaklarının tam kopyasını tutuyor — izlenebilirlik). Uç noktalar: `POST /tickets/{id}/draft`, `GET /tickets/{id}/drafts`, `GET /tickets/{id}`.
- Hafta 4: Güven skoru (`backend/app/services/confidence.py`) — retrieval artık pgvector kosinüs mesafesini de döndürüyor (`retrieve_relevant_chunks_with_distances`), en yakın eşleşmenin benzerliği `confidence_score` olarak kaydediliyor. `ESCALATION_THRESHOLD` (0.5) altındaki taslaklar API yanıtında `needs_escalation=true` ile işaretleniyor (`schemas.py`'de computed field). Onay/düzenleme/red akışı: `PATCH /tickets/{id}/drafts/{draft_id}` (`status`: approved/edited/rejected) — dashboard'da her taslağın altında Onayla/Düzenle/Reddet butonları, düşük güvenli taslaklarda "Dikkatli incele" rozeti.
- Frontend: `/dashboard/tickets/[id]` detay sayfası — talep bilgisi, kategori, "Taslak Oluştur" butonu, üretilen taslak + dayandığı SSS kaynakları + güven skoru + onay aksiyonları. Tarayıcıda uçtan uca test edildi.
- Dashboard'a kategori filtre çubuğu (`?category=` URL param'ı), kategori dağılım özeti ve kategoriye özel renk paleti eklendi (`frontend/lib/categories.ts`, `CategoryFilterBar`, `CategoryDistribution`, `CategoryBadge`). Renk paleti bej (açık mod) + nötr gri-antrasit (koyu mod) + `--accent` bordo marka rengi olacak şekilde ayarlandı.
- **Auth (Hafta 5'ten erken):** Clerk entegre edildi — frontend'de `proxy.ts` (Next.js 16'nın `middleware.ts` yerine geçen adı) `/dashboard` altını girişe kilitliyor, `/sign-in` ve `/sign-up` sayfaları var. Backend'de `backend/app/auth.py`'deki `require_auth` bağımlılığı her iki router'a da uygulandı — `Authorization: Bearer <token>` olmadan `/tickets` ve `/tickets/{id}/draft` uç noktaları 401 dönüyor, `/health` bilinçli olarak açık. Clerk'in "Core 3" sürümü (Mart 2026) `<SignedIn>/<SignedOut>/<Protect>` bileşenlerini kaldırıp `<Show>` ile değiştirmiş — koda bu şekilde yazıldı.
- **Eval seti altyapısı hazır, gerçek çalıştırma Gemini günlük kotasına takıldı:** `eval_examples` tablosu (migration `c265df391716`), 300 gerçek talepten 11 kategorinin hepsini kapsayan 34 elle etiketlenmiş örnek (`scripts/build_eval_set.py` — her biri için "doğru cevap ne olmalı" özeti, SSS kapsamı dışı kalan talepler bilinçli dahil edildi). `backend/run_eval.py`, mevcut sınıflandırma+taslak sistemini bu sete karşı çalıştırıp doğruluk raporu üretiyor; kaldığı yerden devam edebiliyor (`backend/eval_progress.json`, gitignore'da — kota dolup script durursa bir dahaki çalıştırmada zaten tamamlanmış örnekleri tekrar API'ye sormuyor). **Hâlâ tamamlanamadı:** Gemini ücretsiz planının `gemini-2.5-flash` için günlük (dakikalık değil) sadece 20 istek kotası var. 2026-08-20'de (Docker Desktop kapalı olduğu için önce veritabanı `localhost:5432`'ye bağlanamadı, Docker Desktop açılıp `db` container'ı ayağa kalktıktan sonra) script çalıştırıldı — 1 örnek (2 API çağrısı) tamamlandı, sonra yine günlük kotaya takıldı (1/34, ilerleme `eval_progress.json`'da kayıtlı). Kota Pasifik saatine göre gece yarısı sıfırlanıyor; `python run_eval.py` tek komutla kaldığı 1/34'ten devam edecek. `backend/app/services/llm.py`'deki `call_llm` artık 429'da da yeniden deniyor (öncesinde sadece 503'te) — geçici dakikalık sınırlarda işe yarar, günlük kota tükenmesini çözmez.
- **Gelen e-posta webhook'u (Hafta 5'ten erken, backend tarafı hazır ve test edildi):** `POST /webhooks/inbound-email` (`backend/app/routers/webhooks.py`) — Postmark'ın gelen e-posta JSON'unu (`InboundEmailPayload`, `schemas.py`) bir `Ticket`e çeviriyor (`channel="email"`). Clerk yerine HTTP Basic Auth ile korunuyor (`app/auth.py`'deki `verify_webhook_auth`, `WEBHOOK_USERNAME`/`WEBHOOK_PASSWORD`) — çünkü istek bir temsilciden değil, Postmark sunucusundan geliyor. Kendi simüle ettiğim bir payload'la uçtan uca test edildi (401/401/200 senaryoları + gerçek ticket oluşturma), sonra test verisi temizlendi. SendGrid denendi ama vazgeçildi: Inbound Parse, kullanıcının sahip olduğu bir domain (MX kaydı) istiyor; Postmark'a dönüldü çünkü domain'siz ücretsiz bir `@inbound.postmarkapp.com` adresi veriyor. **Not:** Postmark'ın kendisi de kayıt sırasında Gmail/Yahoo gibi genel e-posta adreslerini reddediyor, iş domain'i istiyor — kullanıcı bunu deneyip aştı mı henüz teyit edilmedi. **Henüz yapılmadı:** kullanıcının Postmark hesabı + inbound stream açması, backend'i dışarıya açmak için bir tünel (ngrok) kurması, gerçek bir e-postayla uçtan uca doğrulama.
- **Herkese açık talep formu (Postmark/SendGrid'in domain engeline takılınca eklenen alternatif):** `POST /public/tickets` (`backend/app/routers/public.py`) — hiçbir kimlik doğrulaması istemiyor (gerçek müşterinin Clerk hesabı yok), `EmailStr` ile e-posta formatı doğrulanıyor. Frontend'de `/support` — `/dashboard` gibi Clerk korumasının dışında, herkese açık bir sayfa (`proxy.ts`'in matcher'ı sadece `/dashboard` altını kilitliyor). Tarayıcıda gerçek bir gönderimle uçtan uca test edildi (`channel="form"` ile talep oluştu, dashboard'da göründü), test verisi temizlendi.
- **Rol bazlı giriş sonrası ekran (müşteri portalı + temsilci paneli):** `agents` tablosu (migration `f3a8c91d5e22`) — Clerk kullanıcı kimliği bu tabloda kayıtlıysa temsilci, değilse müşteri sayılır. `backend/app/auth.py`'deki `require_agent` bağımlılığı bunu zorluyor; `/tickets` ve `/tickets/{id}/draft` artık `require_auth` değil `require_agent` istiyor (önceden herhangi bir giriş yapmış kullanıcı, hatta bir müşteri, tüm talepleri ve onaylanmamış AI taslaklarını görebiliyordu — bu açık kapatıldı). Yeni `GET /me` uç noktası (`backend/app/routers/me.py`) `is_agent` döner; frontend'in kök sayfası (`app/page.tsx`) buna göre `/dashboard` ya da `/portal`'a yönlendirir. Müşteri tarafı: `POST/GET /me/tickets`, `GET /me/tickets/{id}` — ad/e-posta talep gövdesinde istenmiyor, taklit edilmesin diye Clerk'ten okunuyor (`backend/app/services/clerk_users.py`). `tickets.submitted_by_user_id` (nullable) bir talebin giriş yapmış hangi müşteriye ait olduğunu tutuyor; anonim `/support` ve e-posta webhook'unda NULL kalıyor. **Önemli:** müşteri yalnızca `status in ("approved", "edited")` olan taslağı görür — `pending`/`rejected` asla sızmaz, bu SQL sorgu seviyesinde zorlanıyor ve ayrı bir testle (`test_get_my_ticket_never_queries_pending_or_rejected_drafts`) doğrulanıyor. Bu, aynı zamanda "onaylanan taslak müşteriye hiçbir zaman gitmiyordu" boşluğunu da kapatan ilk adım — e-posta gönderimi hâlâ yok ama cevap artık müşteri portalında görünüyor. Yeni temsilci eklemek için `scripts/add_agent.py <clerk_user_id> "<Ad Soyad>"` (bkz. aşağıdaki davet linki maddesi — artık bunun self-servis bir alternatifi de var). Tarayıcıda uçtan uca test edildi (müşteri talep açtı → temsilci onayladı → müşteri portalında cevabı gördü), test verisi temizlendi; kullanıcının kendi hesabı ilk temsilci olarak eklendi. Kök sayfa (`/`) artık oturum açık olsa bile otomatik yönlendirme yapmıyor — nötr bir karşılama ekranı ("Hoş geldin, {ad}" + role göre "Panele Git"/"Taleplerime Git" butonu) gösteriyor, kullanıcı isteği: "hangi hesapta kaldığına direkt girmesin".
- **Temsilci davet linki (self-servis, `scripts/add_agent.py`'nin panel karşılığı):** `agent_invites` tablosu (migration `a7c4e9f18b3d`) — bir temsilci `/dashboard/team`'den e-posta girip davet oluşturuyor (`POST /agent-invites`, `backend/app/routers/agent_invites.py`), token `secrets.token_urlsafe(32)`, 7 gün geçerli. Link (`/invite/[token]`) kabul edilince (`POST /agent-invites/{token}/accept`) `agents` tablosuna satır ekleniyor. **Güvenlik:** linki kabul eden kişinin Clerk hesabındaki gerçek e-postası (`clerk_users.fetch_user_profile`) davetteki e-postayla eşleşmiyorsa 403 — linki ele geçiren rastgele biri temsilci olamıyor; bu, tarayıcıda gerçekten yanlış hesapla denenerek doğrulandı. `GET /agent-invites/{token}` (önizleme) kasıtlı olarak yan etkisiz/herkese açık tutuldu — kabul ayrı bir POST, e-posta istemcilerinin linki otomatik önizlemesi yanlışlıkla davet tüketmesin diye. Gerçek e-posta gönderimi yok (proje henüz outbound e-posta'ya sahip değil) — temsilci linki ekranda görüp kendisi paylaşıyor. Bununla birlikte sign-in/sign-up `forceRedirectUrl` yerine `fallbackRedirectUrl` kullanıyor artık — davet linkine tıklayıp oturumu olmayan biri giriş yaptıktan sonra `/`'ye değil, tıkladığı `/invite/...` sayfasına geri dönüyor. Tarayıcıda uçtan uca test edildi (davet oluşturuldu → yanlış hesapla reddedildi → doğru hesapla kabul edilip `agents`'a eklendi), test verisi temizlendi.
- **Geçmiş taleplerin toplu (batch) sınıflandırılması:** Kategori ataması normalde sadece biri "Taslak Oluştur"a bastığında tetikleniyordu (`drafts.py`); 300 talep toplu içe aktarıldığı için (`ingest_kaggle_tickets.py`) hiçbirine taslak üretilmemişti, dolayısıyla ~294'ünün kategorisi hep boştu (dashboard'da %98 "Sınıflandırılmadı"). Tek tek sınıflandırmak (294 ayrı Gemini çağrısı) ücretsiz plandaki günlük 20 istek sınırıyla ~15 gün sürerdi. Bunun yerine `classify_tickets_batch` (`backend/app/services/classification.py`) eklendi — birden fazla talebi TEK LLM çağrısında sınıflandırıyor (`batch_classification_prompt.txt`, modelden `<numara>: <KATEGORİ>` formatında bir satır/talep istiyor; bozuk/eksik satırlar için o talebe özel `OTHER` fallback'i var, tüm batch iptal olmuyor). `backend/classify_backlog.py` bunu `run_eval.py`'deki "ilerlemeyi kaydet, kaldığı yerden devam et" deseniyle (`classify_progress.json`, gitignore'da) 20'lik gruplar hâlinde çalıştırıyor. **Gerçekten çalıştırıldı:** 294 talep sadece 15 batch çağrısında (tek günün kotası içinde, sorunsuz) sınıflandırıldı — artık 300 talebin hepsinin kategorisi dolu, dashboard'daki kategori dağılımı grafiği anlamlı bir dağılım gösteriyor.
- **Dashboard'da "Cevaplandı/Cevaplanmadı" göstergesi:** Talep listesindeki "Durum" sütunu `tickets.status` alanını gösteriyordu — ama bu alan hiçbir zaman güncellenmiyor (yeni taleplerde hep "Açık" kalıyor, seed veride rastgele atanmış), yani gerçek bir bilgi taşımıyordu. Bunun yerine sütun "Cevap" oldu ve müşteri portalındaki (`app/routers/me.py`) "onaylı taslağı var mı" mantığının aynısını kullanıyor: bir talebin `approved`/`edited` durumunda EN AZ BİR taslağı varsa "Cevaplandı" (yeşil), yoksa "Cevaplanmadı" (turuncu). Bu ortak mantık artık `ANSWERED_DRAFT_STATUSES` sabitinde (`backend/app/models/draft_response.py`) tek yerde tutuluyor, hem `me.py` hem `tickets.py` buradan import ediyor. `GET /tickets` ve `GET /tickets/{id}` (`backend/app/routers/tickets.py`), talep başına ayrı sorgu atmak yerine (dashboard tek seferde 300 talebi listeliyor) tek bir SQL sorgusunda `EXISTS` alt sorgusuyla hesaplıyor — performans için önemli. `TicketRead.is_answered` alanı `False` varsayılanla eklendi ki `public.py`/`webhooks.py` gibi taslaksız yeni bir talep döndüren uç noktalar bozulmasın (zaten doğru varsayılan da bu). Talep detay sayfasındaki eski durum rozeti (`TICKET_STATUS_LABELS`/`TICKET_STATUS_STYLES`) dokunulmadan kaldı, sadece dashboard listesindeki sütun değişti.
- **Arayüz Türkçe/İngilizce iki dilli (custom i18n, kütüphanesiz):** AppBar'da bir TR/EN dil seçici var. Hiçbir i18n kütüphanesi eklenmedi (CLAUDE.md'nin "ince soyutlama, ağır framework değil" prensibi) — locale bir cookie'de (`locale`) tutuluyor, URL yapısı değişmedi (`/tr/...` yok). `frontend/lib/i18n.ts` — `Locale`, düz `translations: Record<Locale, Record<key,string>>` sözlüğü (~90 anahtar, `t(locale, key, params?)` ile `{ad}` interpolasyonu destekli), `formatDate`/`formatPercent` (TR `%50` / EN `50%`, tarih `tr-TR`/`en-US`). `frontend/lib/i18n-server.ts` — `getLocale()`, `next/headers` kullanır, SADECE server component'lerden import edilir. `frontend/components/LocaleProvider.tsx` (`"use client"`) — `layout.tsx`'te cookie'den okunan başlangıç değeriyle tüm `children`'ı sarar, `useLocale()` hook'u (`{ locale, t, setLocale }`) döner; `setLocale` cookie'yi günceller ve `router.refresh()` çağırır ki server component'ler de yeni dille yeniden render olsun. `LanguageSwitcher.tsx` bunu kullanır. Server component'ler `getLocale()` + `t(locale, ...)`, client component'ler `useLocale()` kullanır — aynı desen ~25 dosyaya tekrar uygulandı (dashboard, portal, talep detay, taslak paneli, ekip/davet akışı, `/support` formu, karşılama ekranı). Kod→etiket sözlükleri (`CATEGORY_LABELS` — `lib/categories.ts`, `TICKET_STATUS_LABELS` — `TicketsTable.tsx`, taslak/davet durum sözlükleri) locale-keyed oldu, onları okuyan fonksiyonlar `locale` parametresi alıyor. Clerk'in kendi giriş/kayıt/hesap menüsü de değişiyor (`@clerk/localizations`'taki `trTR`/`enUS`, `layout.tsx`'te locale'e göre seçiliyor) — tarayıcıda UserButton menüsünün "Manage account"/"Sign out"a döndüğü doğrulandı. **Kapsam dışı (bilinçli):** talebin kendi metni, AI taslak metni (hâlâ hep Türkçe — `draft_prompt.txt`, ayrı bir konu) ve backend'in `HTTPException` hata mesajları hiç çevrilmiyor — sadece frontend'in kendi ürettiği metinler çevrildi. Ürün adı ("Akıllı Destek Asistanı") bir marka ismi olarak sabit kaldı. Tarayıcıda uçtan uca test edildi: dashboard/portal/talep detay/taslak paneli/ekip/`/support`/karşılama ekranı hepsi İngilizce'ye döndü, tarih formatı değişti, talep metni ve AI taslağı (kapsam dışı olduğu için) Türkçe kaldı, Clerk menüsü İngilizce'ye döndü, Türkçe'ye geri dönüşte her şey normale döndü.
- **Çoklu şirket (multi-tenant) — projenin bugüne kadarki en büyük mimari değişikliği:** Yeni `companies` tablosu (`backend/app/models/company.py`, `slug`+`name`); `tickets`, `knowledge_base_chunks`, `agents`, `agent_invites` artık hepsi `company_id`'ye sahip (migration `b8e2f4a91c67` — mevcut tüm veriyi `slug="genel"` bir "bootstrap" şirkete geriye dönük atadı, isim placeholder, `UPDATE companies SET ...` ile kolayca değiştirilebilir). Bir şirkete "nereden yazılacağı" e-posta tarzı çözüldü — herkese açık bir "şirket seç" listesi YOK (müşterileri birbirine ifşa eder), bunun yerine her şirketin kendi linki var: `/support/{slug}` (anonim form) ve `/portal/new/{slug}` (giriş yapmış müşteri). **Kiracı izolasyonu (en kritik kısım):** `backend/app/services/retrieval.py`'nin ikisi de artık bir `company_id` parametresi alıp `knowledge_base_chunks`'ı ona göre filtreliyor — bir şirketin taslağı başka şirketin SSS'ine asla dayanmaz (gerçek DB'ye karşı çalışan `test_retrieve_relevant_chunks_never_crosses_company_boundary` testiyle doğrulandı). `backend/app/routers/tickets.py` ve `drafts.py`, `require_agent`'ın döndürdüğü `Agent.company_id`'yi kullanıp talep/taslak sorgularını buna göre filtreliyor; başka şirketin talebi 404 (403 değil — var olduğu bile sızdırılmıyor). Bu arada `get_ticket`'taki `is_answered` hesaplamasında gerçek bir hata bulundu ve düzeltildi: `exists()` ifadesi dış sorguda `Ticket` olmadığı için tek bir talebe değil TÜM talep tablosuna bağlanıyordu (yani "herhangi bir talebin onaylı taslağı var mı" sorusuna dönüşüyordu) — artık `ticket_id`'yi literal değer olarak filtreliyor, regresyon testi eklendi. Herkese açık uç noktalar: `GET/POST /public/companies/{slug}(/tickets)`; gelen e-posta webhook'u Postmark'ın `MailboxHash` alanından (adres+slug adresleme) şirketi çözüyor. Yeni şirket + ilk temsilcisini `scripts/create_company.py` oluşturuyor; `scripts/add_agent.py` artık hangi şirkete eklendiğini bilmek zorunda; `scripts/ingest_kb.py --company <slug>` artık SADECE o şirketin SSS'ini silip yüklüyor (önceden tüm tabloyu siliyordu — multi-tenant'ta bu başka şirketlerin verisini yok ederdi, düzeltildi). Frontend: `NewTicketForm`/`SupportForm` artık `companySlug`/`slug` prop'u alıyor; müşteri portalındaki talep listesi (`MyTicketsList`) her satırda küçük bir şirket adı rozeti gösteriyor (bir müşteri artık birden fazla şirkete talep açmış olabilir). Tarayıcıda gerçekten test edildi: ikinci bir test şirketi (`create_company.py` ile) oluşturulup `/support/test-firma` üzerinden gerçek bir talep gönderildi, "genel" şirketinin temsilcisi olarak dashboard'da toplam talep sayısının değişmediği (300'de kaldığı) ve talebe doğrudan URL ile gidildiğinde 404 alındığı doğrulandı — izolasyon gerçekten çalışıyor. Test şirketi/verisi sonda temizlendi.
- **Temel analitik ekranı (Hafta 5'in son parçası):** Yeni `GET /analytics` uç noktası (`backend/app/routers/analytics.py`, `require_agent` korumalı) bir şirketin AI sisteminin performansını özetliyor: talep hacmi (`TicketTotals`: toplam/cevaplanan/taslaksız), taslak durumu dağılımı + ortalama güven skoru + eskalasyon sayısı (`DraftTotals`, `approval_rate` computed field — pending'i paydaya katmıyor) ve günlük talep hacmi (`DailyTicketCount`, en son 30 gün). Üç yardımcı fonksiyon (`_ticket_totals`, `_draft_totals`, `_daily_ticket_counts`), her biri tek bir SQL sorgusunda (`case`/`func.sum` ile) hesaplıyor — talep başına ayrı sorgu yok. `draft_responses` tablosunda `company_id` olmadığı için taslak sorguları `Ticket`'a join'lenip `Ticket.company_id` üzerinden filtreleniyor (kiracı izolasyonu, gerçek DB'ye karşı `test_draft_totals_never_crosses_company_boundary` ve `test_analytics_queries_are_scoped_to_own_company` ile doğrulandı). `tickets.py`'deki `_answered_exists_clause` `answered_exists_clause` olarak paylaşılabilir hale getirildi ("cevaplandı" tanımı tek yerde). **Önemli:** seed veri (Kaggle) 2017 tarihli olduğu için günlük hacim grafiği "bugünden geriye 30 gün" değil, *veride mevcut* en son 30 günü gösteriyor. Frontend: `/dashboard/analytics` (dashboard'daki "Analitik" butonundan), `AnalyticsStats`/`DraftOutcomeChart`/`TicketVolumeChart` bileşenleri (hepsi server component, grafik kütüphanesi yok — CategoryDistribution'daki saf CSS yığılmış-bar deseni tekrar kullanıldı). Taslak durumu renk/etiket sözlükleri `DraftPanel.tsx`'ten `frontend/lib/draftStatus.ts`'e taşınıp paylaşılabilir yapıldı. Tarayıcıda uçtan uca test edildi: sayılar veritabanıyla birebir tutarlı (11 taslak: 6 bekliyor/2 onaylı/2 düzenlenmiş/1 reddedilmiş → onay oranı %80, ortalama güven %56, 5 taslak eskalasyona düşmüş), TR/EN dil değişimi ve yüzde formatı (`%80`/`80%`) doğru çalıştı.
- **Panel yeniden tasarımı — sidebar navigasyon + analitik hero (kullanıcı tasarım geri bildirimi üzerine):** Kullanıcı analitik sayfasının dashboard'la "aynı kart+liste düzeninde" göründüğünü belirtti; üç farklı yön (rapor tarzı/sidebar navigasyon/kendi renk kimliğine sahip bölüm) bir Artifact'te mockup olarak gösterildi, kullanıcı üçünü de beğenip şu birleşimi onayladı: sidebar tüm panel, içerikte banner+tipografik özet cümlesi. **Sidebar:** `frontend/app/dashboard/layout.tsx` (yeni, Next.js nested layout — `/dashboard`, `/dashboard/analytics`, `/dashboard/team`, `/dashboard/tickets/[id]` hepsini sarar; `/portal`, `/support` ayrı route ağaçları oldukları için etkilenmiyor) + `frontend/components/DashboardSidebar.tsx` (yeni, client component — `usePathname()` ile aktif link vurgusu, `/dashboard/tickets/*` da "Talepler" altında aktif sayılıyor). Dashboard'daki üstteki "Analitik"/"Ekip" butonları kalktı. AppBar'a (üst şerit: marka+dil+kullanıcı menüsü) dokunulmadı, tüm site için ortak kalmaya devam ediyor. Mobilde (`md:` altı) sidebar yatay kaydırmalı bir şeride dönüyor — ayrı bir hamburger menü yok. **Analitik hero:** `frontend/components/AnalyticsHero.tsx` (yeni, `AnalyticsStats.tsx`'in yerini aldı — o dosya silindi) — sabit koyu bordo bir banner (`linear-gradient(135deg,#4a2038,#2d1424)`, tema bağımsız, bilinçli bir "marka bandı") içinde tipografik bir özet cümlesi (yeni i18n anahtarı `analytics.thesis`, parametreli: "{total} talepten {answered}'i cevaplandı; üretilen taslakların {rate}'i onaylandı.") + 4 büyük hero-stat. `TicketVolumeChart.tsx` dikey çubuk grafikten SVG alan/çizgi grafiğine çevrildi (path'ler JS'de hesaplanıyor, son nokta vurgulu bir halka ile işaretleniyor). `formatDay` fonksiyonu hem `AnalyticsHero` hem `TicketVolumeChart` kullandığı için `frontend/lib/dateFormat.ts`'e taşındı. `DraftOutcomeChart.tsx`'e dokunulmadı (zaten yatay stacked-bar deseni mockup'larla uyumluydu). Tarayıcıda uçtan uca test edildi: sidebar üç sayfada da (talepler/analitik/ekip) doğru aktif link gösteriyor, talep detayında "Talepler" aktif kalıyor, TR/EN dil değişimi hero cümlesinde ve sidebar etiketlerinde doğru çalıştı.
- **Dashboard genişletmeleri (kullanıcı arayüz geri bildirimi üzerine, 6 bağımsız özellik):** Kullanıcının "arayüzde ne eksik" sorusuna verilen listeden hepsi onaylandı ve uygulandı.
  - **Arama + sayfalama (backend'de, tek uç noktada):** `GET /tickets` artık `q`/`category`/`customer_email`/`page` query paramları alıyor, `TicketListRead` şeması dönüyor (`items` + sayfalama alanları + **filtrelerden bağımsız** şirket-geneli özet: `overall_total`, `open_count`, `classified_count`, `resolved_count`, `category_counts`). Bu ayrım kritik: `TicketStats`/`CategoryDistribution`/`CategoryFilterBar` artık backend'in bu özet alanlarından besleniyor, `items`'tan değil — aksi halde sayfalama/arama sırasında "toplam" sayılar yanlış (sadece o sayfayı) gösterirdi; regresyon testiyle (`test_list_tickets_category_filter_excludes_summary_and_category_counts`) doğrulandı. `frontend/lib/categories.ts`'teki `Ticket[]` alan eski fonksiyonlar (`countTicketsByCategory`, `countsByFixedCategoryOrder`) kaldırılıp backend'in `category_counts` dict'ini işleyen yenileriyle (`categoryDistribution`, `fixedCategoryCounts`) değiştirildi. Yeni `frontend/components/SearchBox.tsx` (client, debounce'lu `?q=`) ve `Pagination.tsx` (server, `?page=`). `frontend/lib/ticketQuery.ts`'teki `buildDashboardHref` üçü de (arama/kategori/sayfa) aynı anda URL'de tutuyor — birini değiştirmek diğerini sıfırlamıyor (sadece `page` her filtre değişiminde 1'e döner).
  - **Toplu onay + toplu taslak oluşturma (kullanıcı geri bildirimiyle iki aşamada genişledi):** `TicketRead`'e `pending_draft_id` eklendi (bir talebin en yeni "pending" taslağının id'si — subquery, `app/routers/tickets.py`'de `pending_draft_id_clause`). Yeni `POST /tickets/bulk-approve` (`app/routers/drafts.py`) birden fazla talebin pending taslağını tek seferde onaylıyor; kiracı izolasyonu (başka şirketin id'si `skipped`'e düşer, asla `approved`'e girmez) ve "birden fazla pending taslak varsa sadece en yenisi onaylanır" davranışı gerçek bir tarayıcı testinde de doğrulandı. `TicketsTable.tsx` server'dan **client component'e** çevrildi (`DraftPanel.tsx` deseni: `useAuth`+`useLocale`). **İlk halinde** checkbox SADECE pending taslağı olan taleplerde görünüyordu — kullanıcı ekran görüntüsüyle "çoğu satırda seçim kısmı yok" diye bildirdi, araştırınca bunun bilinçli bir tasarım olduğu ama kullanıcının farklı bir şey istediği ortaya çıktı: **hiç taslağı olmayan** talepler için de toplu işlem yapabilmek. Bunun üzerine yeni `POST /tickets/bulk-generate-drafts` eklendi (`BulkGenerateResult`: `created`/`skipped`/`failed`) — her ticket için gerçek bir Gemini çağrısı yapar, kota/ağ hatası olan talep `failed`e düşer ama işlem DURMAZ (kısmi başarı toleranslı, aynı `bulk_approve_tickets` prensibi). Artık checkbox `is_answered` olmayan (yani "Cevaplanmadı") HER talepte aktif; sadece zaten cevaplanmış olanlarda disabled. `TicketsTable.tsx` seçili talepleri iki gruba ayırıyor (`pending_draft_id !== null` → onay grubu, `=== null` → oluşturma grubu) ve duruma göre bir ya da iki buton gösteriyor ("N Onayla" / "N için Taslak Oluştur") — karışık seçimde ikisi birden görünür, her biri sadece kendi grubunu işler. Gerçek bir toplu taslak oluşturma tarayıcıda test edildi: `docker exec ... psql` ile `draft_responses`'a gerçek bir satırın (durum `pending`, gerçek güven skoru ve metin) eklendiği doğrulandı.
  - **Müşteri geçmişi:** Talep detay sayfası artık `fetchTickets(token, {customerEmail})` ile aynı müşterinin diğer taleplerini çekip küçük bir liste olarak gösteriyor (sonuç boşsa bölüm hiç görünmüyor). Gerçek Kaggle verisinde hiçbir müşterinin birden fazla talebi olmadığı fark edildi (test için geçici 2 kayıt eklenip silindi) — özellik çalışıyor ama bu veri setiyle organik olarak hiç tetiklenmeyecek.
  - **Sidebar sticky:** `AppBar` sabit `h-14` yüksekliğe alındı, `DashboardSidebar` `md:sticky md:top-14` oldu — AppBar'ın kendi sticky'siyle çakışmadan ikisi birlikte sabit kalıyor.
  - **Tema anahtarı (Sistem/Açık/Koyu):** `frontend/lib/theme.ts` + `ThemeProvider.tsx` + `ThemeSwitcher.tsx` (`LanguageSwitcher`/`LocaleProvider` deseninin birebir kopyası). **İlk denemede** (`next/script strategy="beforeInteractive"` ile `localStorage`'dan flash-önleme) React 19'un konsoluna "Encountered a script tag while rendering React component" hatası düştü — kullanıcı bunu fark edip bildirdi. **Düzeltme:** tema de tıpkı `locale` gibi **cookie**'de tutulacak şekilde değiştirildi — yeni `frontend/lib/theme-server.ts` (`getTheme()`, `i18n-server.ts` deseni, `next/headers`) `app/layout.tsx`'te server-side okunup `<html data-theme>`'i İLK render'da doğru üretiyor, hiçbir client-side script'e gerek kalmadı. `ThemeProvider` artık `initialTheme` prop'u alıyor (`LocaleProvider`'ın `initialLocale`'i gibi), `setTheme` cookie yazıp `router.refresh()` çağırıyor. `globals.css`'e `:root[data-theme="dark"]` bloğu + mevcut `@media (prefers-color-scheme: dark)` artık `:root:not([data-theme="light"])` ile guard'lı (elle "Açık" seçimi sistem koyu modunu ezebilsin diye). **Ders:** client-only bilgiyi (tema, dil) `<html>`'e SSR öncesi bir script ile enjekte etmeye çalışmak yerine, proje zaten cookie+server-read deseni kurmuşsa (i18n) aynı deseni tekrar kullanmak hem daha az kod hem daha az kırılgan.
  - **Karşılaşılan ve düzeltilen gerçek bug:** `TICKET_STATUS_LABELS`/`TICKET_STATUS_STYLES` sabitleri `TicketsTable.tsx`'te tanımlıydı; o dosya `"use client"` olarak işaretlenince, bu sabitleri import eden server component (`dashboard/tickets/[id]/page.tsx`) çalışma zamanında "Cannot read properties of undefined" hatası verdi — Next.js'in RSC derleyicisi `"use client"` işaretli bir dosyanın TÜM export'larını (component olmayanlar dahil) client-reference'a çeviriyor. Çözüm: paylaşılan sabitler `"use client"` İÇERMEYEN ayrı bir dosyaya (`frontend/lib/ticketStatus.ts`) taşındı — `draftStatus.ts`'teki mevcut desenin aynısı. **Genel kural:** bir client component dosyasından sabit (non-component) export etmek riskli, paylaşılan sabitler her zaman `lib/` altında ayrı dosyalarda tutulmalı.
  - Backend: `test_bulk_approve.py` (3 test) + `test_bulk_generate_drafts.py` (5 test, `google.genai.errors.APIError` gerçek constructor imzasıyla mock'lanıyor — kota hatası senaryosu dahil) + `test_tickets.py`'nin tamamı yeni `TicketListRead` yanıt şekline göre yeniden yazıldı — toplam 81 test, hepsi geçiyor. Tarayıcıda uçtan uca test edildi: arama (`?q=iphone` sırasında genel istatistiklerin değişmediği doğrulandı), sayfalama (6 sayfa, `?page=2`), toplu onay + toplu taslak oluşturma (ikisi de gerçek veritabanı satırıyla `docker exec ... psql` doğrulandı), müşteri geçmişi (geçici test verisiyle), sidebar sticky, tema anahtarının üç durumu da (flash olmadan, cookie tabanlı).
- 81 birim testi (`backend/tests/`), hepsi geçiyor.

Not: `.env`'deki anahtar adı `GEMINI_API_KEY` (önceki `ANTHROPIC_API_KEY` adlandırması hataydı, düzeltildi). `clerk-backend-api` kurulumu `pydantic`'i 2.10.4'ten 2.13.4'e yükseltti, testler bununla uyumlu.

**Ortam notu:** Bu oturumda backend'in 8000 portunda hiçbir araçla (Windows süreç yöneticisi, tasklist, Bash ps) görülemeyen/öldürülemeyen "hayalet" bir süreç sıkışıp kaldı. Backend şu an **8001** portunda çalışıyor, `frontend/.env.local`'daki `NEXT_PUBLIC_API_URL` buna göre güncellendi. Bilgisayar yeniden başlatılınca 8000 muhtemelen düzelir; o zaman istenirse geri 8000'e alınabilir (opsiyonel). **Önemli:** backend'i elle başlatırken mutlaka `--reload` ile başlat (`uvicorn main:app --host 0.0.0.0 --port 8001 --reload`) — bir oturumda `--reload` olmadan başlatılmış, sonra kod değişmiş, ve değişikliklerin arayüze yansımaması kısa bir "neden çalışmıyor" hata ayıklamasına yol açmıştı (backend'i yeniden başlatınca çözüldü).

**Eval seti ve gerçek e-posta entegrasyonu — kullanıcı kararıyla şimdilik bırakıldı (2026-08-21):**
- **Eval seti:** Gemini ücretsiz planının günlük 20 istek kotası 34 örneklik seti bitirmeyi günler alacak şekilde yavaşlattığı için kullanıcı script'i durdurup ilerlemeyi olduğu gibi (1/34, `backend/eval_progress.json`) bırakmayı tercih etti. İleride istenirse `python run_eval.py` kaldığı yerden devam eder; alternatif olarak Gemini'de faturalandırma açmak (maliyet muhtemelen birkaç kuruş, ~68 istek) ya da eval setini küçültmek de konuşuldu ama uygulanmadı.
- **Gerçek e-posta entegrasyonu:** Sırasıyla SendGrid, Postmark ve Mailgun denendi, üçü de aynı temel engele takıldı — kullanıcının sahip olduğu bir domain (MX kaydı) veya "iş" e-postası istiyorlar, kullanıcının hiçbiri yok. Alternatif olarak **IMAP polling** (backend'in bir Gmail hesabını periyodik olarak kontrol etmesi — domain, webhook veya ngrok tüneli gerektirmez, sadece Google "Uygulama Şifresi" gerekir) önerildi ve kullanıcıya anlatıldı, ancak kullanıcı bunu da "şimdilik atlayalım, ileride belki bakarız" diyerek erteledi. **Şu an e-posta girişi için tek çalışan yollar:** `/support/{slug}` (herkese açık form) ve müşteri portalı (`/portal/new/{slug}`) — backend'deki webhook kodu (`backend/app/routers/webhooks.py`, Postmark formatına göre yazılmıştı) kullanılmıyor ama silinmedi, ileride IMAP polling'e veya başka bir sağlayıcıya geçilirse yeniden ele alınabilir.

- **Lead qualification (Hafta 6'nın ilk parçası):** Gelen bir talebin aslında bir destek sorunu değil de bir SATIŞ FIRSATI (toplu/kurumsal alım isteği, fiyat/paket teklifi, yeni müşteri ilgisi) olup olmadığını tespit ediyor. **Tasarım kararı:** ayrı bir servis/LLM çağrısı YAPILMADI — Gemini'nin ücretsiz günlük 20 istek kotası bu oturumda defalarca sorun çıkardığı için (bkz. yukarıdaki eval notu), lead tespiti sınıflandırmayla AYNI LLM çağrısına eklendi (`backend/app/services/classification.py` — `classify_ticket`/`classify_tickets_batch` artık düz bir kategori string'i değil, `ClassificationResult(category, is_lead)` dönüyor; prompt'lar iki satırlık bir format istiyor: `KATEGORİ: <kategori>` / `LEAD: EVET|HAYIR`, batch'te `<numara>: <KATEGORİ> | <EVET/HAYIR>`). `tickets.is_lead` (migration `dcf794d53bf3`), `GET /tickets`'e `is_lead` filtresi eklendi (bugünkü `channel`/`is_answered` deseniyle birebir aynı). Frontend: `TicketFilterBar`'a "Fırsat: Tümü/Potansiyel Satış" çipi, `TicketsTable` ve talep detay sayfasında "Potansiyel Satış" rozeti (`bg-accent-soft text-accent`, StatusPill). **Kapsam bilinçli olarak dar tutuldu** (Erken optimizasyona hayır) — CRM'e otomatik gönderim yok, ayrı bir "satış kuyruğu" yok, sadece tespit + görünürlük + filtre. Tarayıcıda gerçek bir talebi `docker exec ... psql` ile `is_lead=true` yapıp doğrulandı (filtre, liste rozeti, detay rozeti), sonra test verisi geri alındı. 90 backend testi (81 → 90), hepsi geçiyor.

- **Tool-use (Hafta 6'nın ikinci parçası):** Taslak yazarına, statik SSS metninin ötesinde gerçek bir şeye ("bu müşterinin bu şirkete başka talebi var mı") bakma yetkisi verildi. **Mekanizma:** google-genai SDK'sının "otomatik fonksiyon çağırma" özelliği kullanıldı — elle bir tool-calling döngüsü YAZILMADI. `backend/app/services/llm.py`'ye yeni `call_llm_with_tools(prompt, context, tools)` eklendi (`call_llm`'le aynı retry mantığını `_generate_with_retries` adlı paylaşılan bir yardımcıdan alıyor); `tools` düz Python fonksiyonlarından oluşan bir liste, SDK modelin bunlardan birini çağırmak isteyip istemediğine bakıp gerekirse fonksiyonu KENDİSİ çalıştırıyor. `backend/app/services/draft_generation.py`'de `generate_draft` içinde tanımlı bir closure (`get_customer_ticket_history`) — `ticket`/`db`'yi kapatarak modelin company_id/customer_email gibi şeyleri kendi uydurmasının/parametre olarak istemesinin önüne geçiyor, model sadece "çağır" diyebiliyor. `DraftResult`/`draft_responses` tablosuna `used_customer_history: bool` eklendi (migration `38a435c6833a`) — modelin bu aracı GERÇEKTEN çağırıp çağırmadığının izlenebilirlik kaydı (retrieved_context'in şeklini bozmamak için ayrı bir alan olarak tutuldu, KB kaynaklarıyla karıştırılmadı). Frontend: `DraftPanel`'de taslak, müşteri geçmişine bakılarak yazıldıysa küçük bir not gösteriyor. **Gerçek Gemini API'sine karşı doğrulandı** (`backend/app/db/database.py` üzerinden gerçek bir talebe `generate_draft` çağrılarak, ayrıca tarayıcıda gerçek "Taslak Oluştur" butonuyla uçtan uca) — SDK entegrasyonu hatasız çalışıyor, model bu iki denemede aracı çağırmamayı seçti (optional bir araç olduğu için bu geçerli bir davranış, zorunlu değil). Aracın kendisinin doğruluğu (doğru müşteri/şirkete göre filtrelemesi, boş/dolu durumları) gerçek bir model çağrısı gerektirmeden unit testlerle doğrulandı (`tests/test_draft_generation.py`, mocklanmış `call_llm_with_tools`'a iletilen fonksiyon doğrudan çağrılarak). Yeni `tests/test_llm.py` (4 test) `call_llm`/`call_llm_with_tools`'un retry davranışını ve `tools`'un config'e doğru iletildiğini doğruluyor. 97 backend testi (90 → 97), hepsi geçiyor.

- **Landing page (roadmap dışı, kullanıcı arayüz geri bildirimi üzerine):** Kök sayfanın (`frontend/app/page.tsx`) oturum kapalıyken gösterdiği nötr "Hoş geldin" ekranı gerçek bir tanıtım sayfasına dönüştürüldü. Üç yön (klasik SaaS / ürünü göster / anlatı) bir Design canvas'ında (Artifact) mockup olarak sunuldu, kullanıcı **Yön B — Ürünü Göster**'i seçti: asimetrik split hero + sağda gerçek onay panelinin (DraftPanel) birebir statik mockup'ı (rozet, güven yüzdesi, SSS kaynağı, Onayla/Düzenle/Reddet butonları — hepsi görsel, işlevsiz) + altında "talep→AI taslak hazırlar→insan onaylar" akış şeridi + "neden sadece AI ya da sadece insan yetmez" bölümü + koyu bordo kapanış CTA bandı (AnalyticsHero'daki sabit marka bandı deseninin tekrarı). Yeni `frontend/components/LandingPage.tsx` (server component, mevcut `t()`/`getLocale()`/`StatusPill`/`DRAFT_STATUS_PILL_CLASSES` bileşenlerini yeniden kullanıyor, yeni bir bağımlılık eklemedi), i18n'e ~13 yeni `landing.*` anahtarı (TR+EN) eklendi. `app/page.tsx`'in signed-out dalı artık doğrudan `<LandingPage />` döndürüyor; signed-in dalı (role göre `/dashboard`/`/portal`'a yönlendiren ayrı karşılama) değişmedi. `npx tsc --noEmit` temiz; dev sunucusunda `curl` ile 200 ve beklenen içerik doğrulandı, tarayıcıda henüz manuel/görsel test edilmedi (uzantı bağlı değildi).

- **Marka yenilemesi — "supportIQ" (roadmap dışı):** Ürün adı "Akıllı Destek Asistanı"dan **supportIQ**'ya değiştirildi — site genelinde (navbar, sayfa başlığı/meta, davet ekranı başlığı, backend'in FastAPI/OpenAPI başlığı). Yeni `frontend/components/Logo.tsx`: `LogoMark` (destek diyalog balonu + içine gömülü, düğüm noktalı yükselen bir analitik/sinyal çizgisinin birleşimi — "destek + AI analitiği" konsepti, tamamen `var(--foreground)`/`var(--accent-tech)` CSS değişkenleriyle çizilmiş SVG, tema değişince otomatik uyum sağlıyor) ve `Wordmark` ("support" nötr `text-foreground`, "IQ" vurgulu `text-accent-tech`). Yeni CSS değişkeni `--accent-tech` (`#6c63ff`, mor-indigo) — bilinçli olarak açık/koyu tema arasında SABİT tek bir değer (mevcut `--accent` bordo gibi tema başına ayrı değer değil), çünkü bir logo rengi tema değişince değişmemeli; `globals.css`'teki `@theme inline`'a `--color-accent-tech` olarak eklendi. **Kapsam bilinçli dar tutuldu:** yalnızca marka ADI ve site geneli logo değişti; dashboard/portal'ın kendi bej+bordo renk paleti ve bileşen stilleri DOKUNULMADI (kullanıcı isteği açıkça "mevcut landing page" ile sınırlıydı).
- **Landing page — premium B2B/SaaS revizyonu:** `LandingPage.tsx` (Yön B — bkz. yukarıki not) Linear/Stripe/Datadog estetiğine göre yeniden tasarlandı. Hero ve kapanış CTA'sı artık AnalyticsHero'daki "tema bağımsız sabit marka bandı" desenini tekrarlıyor — sabit koyu gradyan (`#0b0b10→#14121f→#0d0d14`), arkada iki blur'lu renkli "glow" (accent-tech mor + bordo), ve DraftPanel mockup kartı gerçek bir **cam efekti (glassmorphism)** ile (`backdrop-blur-xl`, yarı saydam beyaz gradyan zemin, mor glow'lu box-shadow) render ediliyor. Bu bölümler koyu/camsı olduğu için içindeki metin renkleri site temasından (light/dark) bağımsız olarak elle beyaz/beyaz-şeffaf tonlarda sabitlendi — aksi halde DraftPanel'in normalde açık zemine göre tasarlanmış pastel durum rozeti renkleri (`--status-open-bg` vb.) koyu site temasında koyu-üstüne-koyu görünüp okunmaz hâle gelirdi; bu yüzden mockup'taki "Onay bekliyor" rozeti `DRAFT_STATUS_PILL_CLASSES`'tan değil, yeni bir `accent-tech` tabanlı sabit rozetten geliyor. Journey adımları (`JourneyStep`) ve "neden AI+insan" bölümü açık/nötr kart tasarımına (border+shadow, `accent-tech` tonlu ikon rozeti) yükseltildi. Yeni i18n anahtarı `landing.eyebrow` (TR/EN) eski "marka adını tekrar eden" eyebrow etiketinin yerini aldı — navbar'da zaten logo göründüğü için heroda tekrar marka adı yazmak yerine kısa bir kategori etiketi ("Yapay zekâ destekli müşteri hizmetleri") kullanıldı. `npx tsc --noEmit` ve `npx eslint` temiz; dev sunucusunda `curl` ile 200 ve beklenen içerik doğrulandı, tarayıcıda henüz manuel/görsel test edilmedi (uzantı bağlı değildi).
- **Panel/portal giriş noktaları artık önce landing page'e düşüyor:** `frontend/proxy.ts` — oturumsuz biri `/dashboard(.*)` veya `/portal`/`/portal/tickets(.*)`'a girerse artık doğrudan Clerk'in giriş ekranına değil, önce landing page'e (`/`) yönleniyor (`auth.protect({ unauthenticatedUrl: ... })`). Bağlam taşıyan linkler (`/invite/{token}`, `/portal/new/{slug}`) bilinçli olarak dışarıda bırakıldı — onlar zaten hangi hesaba/şirkete gideceğini biliyor, doğrudan giriş ekranına gidip işlem sonrası aynı linke (`redirect_url`) geri dönmeye devam ediyor. `curl` ile üç senaryo da (`/dashboard`→`/`, `/portal`→`/`, `/portal/new/genel`→`/sign-in?redirect_url=...`) doğrulandı.

- **Giriş-sonrası karşılama ekranı artık bir gerçek özet gösteriyor (kullanıcı geri bildirimi: "çok boş duruyor"):** `app/page.tsx`'in signed-in dalı artık salt "Hoş geldin + buton" değil — panele/portala gitmeden önce tek bir gerçek veri özeti gösteriyor. Temsilci için mevcut `/analytics` uç noktasından `drafts.pending` ve `tickets.answered/total` iki küçük istatistik kutusu olarak; müşteri için mevcut `/me/tickets`'ten en yeni talebi (`latestTicket`) başlığı + cevaplandı/bekliyor rozetiyle (StatusPill + DRAFT_STATUS_PILL_CLASSES, mevcut yeşil/turuncu renkler yeniden kullanıldı) küçük tıklanabilir bir kart olarak gösteriliyor. **Yeni bir backend uç noktası eklenmedi** — ikisi de zaten var olan endpoint'lerin (fetchAnalytics/fetchMyTickets) tekrar kullanımı. Müşterinin hiç talebi yoksa kart hiç görünmüyor (boş durum için ayrı bir tasarım yapılmadı, bilinçli olarak basit tutuldu). Yeni i18n anahtarları: `welcome.statPendingDrafts`, `welcome.statAnsweredTickets`, `welcome.latestTicketLabel`, `welcome.ticketAnswered`, `welcome.ticketPending` (TR/EN).

- **Taslak düzenleme farkının kaydı (roadmap dışı, önceden konuşulup askıya alınmış bir üretimleşme fikriydi — bkz. proje hafızası):** `draft_responses`'a yeni, DEĞİŞMEZ bir sütun: `ai_original_text` (migration `6039796fb7fb`). AI ilk taslağı ürettiğinde (`create_draft` VE `bulk_generate_drafts`, `backend/app/routers/drafts.py`) `ai_original_text` `draft_text` ile AYNI değerle set ediliyor; bir temsilci taslağı düzenleyip (`status="edited"`) kaydettiğinde sadece `draft_text` değişiyor, `ai_original_text` hep AI'nin ilk çıktısı olarak kalıyor — böylece "AI ne önerdi" vs "insan ne değiştirdi" farkı hiç kaybolmuyor. Mevcut satırlar geriye dönük olarak `draft_text`'in o anki kopyasıyla dolduruldu (gerçek orijinal zaten saklanmıyordu — bu satırlar için "hiç düzenlenmedi" gibi görünecekler, bilinen bir sınır). Frontend: `DraftPanel.tsx`'te bir taslak `status="edited"` VE metni AI'nin orijinalinden farklıysa, taslağın altında "AI'nin orijinal önerisini göster" linki beliriyor — tıklanınca kesikli çerçeveli bir kutuda orijinal metni gösteriyor (salt okunur, sadece karşılaştırma amaçlı). Yeni `tests/test_drafts.py` (3 test) hem oluşturmada `ai_original_text == draft_text` eşitliğini hem düzenleme sonrası ayrışmayı hem de düz onaylamanın hiçbir metni değiştirmediğini doğruluyor. 97 backend testi (97 → 100), hepsi geçiyor. Gerçek DB'ye karşı da doğrulandı (`docker exec ... psql`: 13 taslağın hepsinde `ai_original_text` dolu, geriye dönük satırlarda beklendiği gibi `draft_text = ai_original_text`).

- **"Özgün 10 özellik" listesi #1 — Bilgi tabanı boşluk tespiti:** Yeni `GET /analytics/knowledge-gaps` (`backend/app/routers/analytics.py`, `require_agent` korumalı) — hangi kategoride AI'nin sık sık düşük güvenle/eskalasyona düşerek taslak ürettiğini (yani SSS'te muhtemelen eksik olan konuyu) gösterir. `_knowledge_gaps` fonksiyonu `Ticket.category`'ye göre gruplayıp eskale taslak sayısını + toplam sayıyı hesaplıyor, en az bir eskalasyonu olan kategoriler için en yeni 3 örnek talep başlığını da (tekilleştirilmiş, aynı talep iki taslağa sahipse tekrar etmesin diye Python'da dedupe edilmiş) ekliyor. `_draft_totals`'taki eskalasyon tanımı (`confidence_score is None OR < ESCALATION_THRESHOLD`) `_escalated_clause()` adında paylaşılabilir bir yardımcıya çıkarıldı (iki yerde aynı eşik mantığının tekrarını önlemek için). Yeni `KnowledgeGapItem` şeması. Frontend: `/dashboard/analytics`'e yeni `KnowledgeGaps.tsx` kartı (CategoryBadge yeniden kullanılıyor) — kategori + "N/M taslak düşük güvenli" + örnek başlıklar. 6 yeni backend testi (gerçek DB'ye karşı, `two_companies` fixture deseniyle — gruplama, sıfır-eskalasyonlu kategorinin dışlanması, kiracı izolasyonu). 103 backend testi, hepsi geçiyor. **Not:** bu oturumda backend'in `--reload` süreci main.py değişikliğinden sonra sessizce takılıp kalmış (WatchFiles "main.py değişti" uyarısını verdi ama yeni worker hiç başlamadı, `/openapi.json` eski koda göre yanıt vermeye devam etti) — fark edilince süreç durdurulup temiz başlatıldı, yeni uç nokta doğrulandı. **Sıradaki 9 madde için not:** eğer bir kod değişikliğinden sonra backend garip/eski davranıyorsa önce `curl .../openapi.json`'da beklenen alan/yolun göründüğünü doğrula, gerekirse `--reload` sürecini tamamen yeniden başlat.

- **"Özgün 10 özellik" listesi #2 — Çözülen taleplerden otomatik SSS önerisi:** Yeni `kb_suggestions` tablosu (migration `7d20826fb1f5` — `company_id`, `ticket_id`, `question`, `answer`, `category`, `intent`, `status` pending/approved/rejected, `kb_chunk_id` nullable). Yeni servis `backend/app/services/kb_suggestion_generation.py` — `generate_kb_suggestion(subject, body, final_answer)`, `call_llm` ile (sınıflandırma gibi `SORU:`/`CEVAP:`/`KOD:` etiketli, her etiket kendi satırında olacak biçimde) talebi genelleştirilmiş bir SSS maddesine çeviriyor; format bozulursa (etiket eksikse) sınıflandırmadaki `FALLBACK_CATEGORY` prensibiyle aynı şekilde makul varsayılanlara (konu/nihai yanıt/"genel") düşülüyor, hiçbir zaman tüm işlem iptal olmuyor. Yeni router `backend/app/routers/kb_suggestions.py` (prefix `/tickets`, drafts.py ile birebir aynı desen): `POST /tickets/{id}/kb-suggestion` (üret, talebin en yeni onaylı/düzenlenmiş taslağının metnini kullanır, hiç yoksa 400), `GET /tickets/{id}/kb-suggestions` (listele), `PATCH /tickets/{id}/kb-suggestions/{sid}` (onayla/reddet, onaylamadan önce soru/cevap düzenlenebilir — DraftStatusUpdate ile aynı desen). **Kritik:** `POST /tickets/{id}/kb-suggestion` **knowledge_base_chunks'a HİÇBİR ŞEY eklemez** — sadece `kb_suggestions`'a `status="pending"` bir kayıt düşer; gerçek SSS kaydı SADECE `PATCH .../approved` ile, bir insan onayladığında oluşur (embedding `scripts/embed_kb.py` ile aynı `soru+cevap` deseniyle senkron hesaplanır, `source="ticket_suggestion"`) — "İnsan onaylı akış" ilkesi burada bilgi tabanına eklenen içerik için de uygulandı, sadece müşteriye giden yanıtlar için değil. Frontend: yeni `KbSuggestionPanel.tsx` (DraftPanel ile aynı client-component deseni, `accent-tech` rengiyle görsel olarak "AI/analitik" hissi taşıyan diğer öğelerden ayrışıyor) — talep detay sayfasında DraftPanel'in hemen altında, sadece `ticket.is_answered` iken "SSS Önerisi Oluştur" butonu görünüyor. 9 yeni backend testi (3 servis-seviyesi ayrıştırma testi + 6 router-seviyesi, mocklanmış DB — `test_drafts.py` deseni: onaylı yanıt yoksa 400, kiracı izolasyonu, onayda gerçek `KnowledgeBaseChunk` oluşup embed edildiğinin doğrulanması, düzenlenmiş soru/cevabın kullanılması, reddedilince hiçbir SSS kaydı oluşmaması). 112 backend testi, hepsi geçiyor. Gerçek DB'ye karşı da doğrulandı (`docker exec ... psql \d kb_suggestions`).

- **"Özgün 10 özellik" listesi #3 — CSAT mikro-tepkisi:** `draft_responses`'a yeni `customer_reaction: str | None` sütunu (migration `5ca30d7a4e9a`, `"up"`/`"down"`/NULL). Yeni `PATCH /me/tickets/{id}/reaction` (`backend/app/routers/me.py`, `require_auth` — müşteri kendi uç noktası, agent değil) talebin en yeni onaylı/düzenlenmiş taslağına tepkiyi yazıyor; talebin hiç yanıtı yoksa 400. Aynı tepkiye tekrar basmak `reaction: null` göndererek temizliyor (toggle). `me.py`'de `_latest_answer` (sadece metin döndüren) `_latest_answered_draft` (tüm satırı döndüren) olarak genişletildi — hem `answer` hem `reaction` tek sorgudan türetiliyor. Tekrar eden "talep gerçekten bana mı ait" kontrolü `_get_own_ticket` adında paylaşılabilir bir yardımcıya çıkarıldı (get_my_ticket + update_ticket_reaction). `MyTicketRead`'e `reaction`, `DraftResponseRead`'e (agent tarafı görünürlüğü için) `customer_reaction` eklendi. Frontend: yeni `TicketReactionButtons.tsx` (client component) — müşteri portalında yanıtın altında 👍/👎, `DraftPanel.tsx`'te de temsilciye "Müşteri bu yanıtı yararlı buldu/bulmadı" notu (varsa) gösteriliyor — ekip dışından gelen ilk gerçek AI-kalite sinyali artık hem müşteriye hem temsilciye görünür. 6 yeni backend testi (kiracı/sahiplik izolasyonu, yanıtsız talepte 400, tepki yazma, `null` ile temizleme) + 1 mevcut testin (`test_get_my_ticket_surfaces_approved_answer`) mock'u yeni sorgu şekline göre güncellendi. 116 backend testi, hepsi geçiyor. Gerçek DB'ye karşı da doğrulandı (`\d draft_responses` içinde `customer_reaction` sütunu).

- **"Özgün 10 özellik" listesi #4 — Duygu/aciliyet tespiti:** Lead tespitiyle BİREBİR aynı desen (`is_lead` ile aynı LLM çağrısı, ek kota maliyeti yok) — sınıflandırma prompt'larına (`classification_prompt.txt`, `batch_classification_prompt.txt`) üçüncü bir soru eklendi: talep sinirli/tehdit edici bir tonda mı, güvenlik/dolandırıcılık şüphesi mi var, zamana duyarlı ciddi bir sorun mu ("param çekildi ama siparişim yok" gibi)? Etiket bilinçli olarak İngilizce **"URGENT"** seçildi (LEAD gibi) — Türkçe "ACİL" kelimesindeki noktalı büyük İ, Python'un `.upper()`'ının ürettiği dotless I ile eşleşmiyor (bkz. KATEGORİ'deki aynı, zaten dokümante edilmiş tuzak); URGENT bu sorunu tamamen ortadan kaldırıyor. `ClassificationResult`e `is_urgent: bool = False` eklendi, hem tekli hem batch parser güncellendi. `tickets.is_urgent` (migration `d2bb41357916`, `is_lead` ile aynı desen) — `create_draft`, `bulk_generate_drafts` (`drafts.py`) ve `classify_backlog.py`'de `is_lead` atandığı her yerde `is_urgent` da atanıyor. **Karşılaşılan bug:** `public.py`/`webhooks.py`/`me.py`'de yeni talep oluşturan üç yerde `Ticket(...)` çağrısı `is_lead=False`'ı elle set ediyordu ama `is_urgent`'ı etmiyordu — SQLAlchemy'nin `default=False`'u sadece flush/commit anında uygulanıyor, commit öncesi/mocklanmış testlerde `is_urgent` `None` kalıp `TicketRead` response validasyonunu (bool bekliyor) patlatıyordu; üçüne de `is_urgent=False` eklenerek düzeltildi. `GET /tickets`e `is_urgent` filtresi eklendi (`is_lead` ile birebir aynı desen). Frontend: `TicketFilterBar`'a "Aciliyet: Tümü/Acil" çipi (kırmızı `bg-red-50 text-red-700` — lead'in mor `accent-soft` rengiyle karışmasın diye bilinçli farklı renk), `TicketsTable` + talep detayında "Acil" rozeti; `buildDashboardHref`/`CategoryFilterBar`/`Pagination` zincirine `urgent` parametresi eklendi (diğer filtreler değişince aciliyet filtresi sessizce sıfırlanmasın diye — `lead`'in izlediği aynı "her link tüm boyutları taşır" deseni). 8 yeni backend testi (tekli+batch ayrıştırma, `GET /tickets?is_urgent=` filtresi). 121 backend testi, hepsi geçiyor. Gerçek DB'ye karşı da doğrulandı.

- **"Özgün 10 özellik" listesi #5 — Talep sahiplenme (assignment/claim):** `tickets.assigned_agent_id` (migration `ac6dd06c3067`, FK `agents.id`, nullable, indexli). Yeni `PATCH /tickets/{id}/assignment` (`backend/app/routers/tickets.py`) — `claim=true` talebi ARAYAN temsilciye atar (başkasına atanmış olsa bile devralma serbest, küçük ekipte yeterli — 409 çakışma mantığı bilinçli olarak eklenmedi), `claim=false` atamayı kaldırır. `TicketRead`e `assigned_agent_id` (gerçek sütun, otomatik gelir) ve `assigned_agent_name` (is_answered gibi ayrıca çözülüp doldurulan alan) eklendi — bunun için yeni `assigned_agent_name_clause()` (list_tickets'ın tek sorgusuna 3. bir correlated subquery olarak eklendi, `pending_draft_id_clause` ile aynı desen). **Refactor:** `get_ticket`in is_answered/pending_draft_id hesaplama mantığı, yeni assignment uç noktasıyla ikinci kez tekrar etmesin diye `_read_ticket_with_computed_fields(ticket, db)` adında paylaşılabilir bir yardımcıya çıkarıldı. **Karşılaşılan bug (aynı sınıf, is_urgent'takiyle):** `public.py`/`webhooks.py`/`me.py`'nin `Ticket(...)` çağrılarında `assigned_agent_id` set edilmiyordu; sorun olmadı çünkü ilgili response şemaları (`TicketRead`/`MyTicketRead`) bu alana `None` varsayılanıyla toleranslı — is_urgent'takinden farklı olarak burada patlama olmadı (nullable alan, non-nullable bool değil). Frontend: yeni `TicketAssignmentButton.tsx` (client component) — talep detayında "Kimse üstlenmedi"/"Sen üstlendin"/"{isim} üstlendi" + Üstlen/Devral/Bırak butonları; "sen" ayrımı Clerk'in `useUser().fullName`'ini `assigned_agent_name`'le karşılaştırarak yapılıyor, ekstra bir API çağrısı gerekmedi. `TicketsTable`'da liste satırında da atanan kişinin adı küçük bir ek bilgi olarak görünüyor (yeni bir sütun açmadan, mevcut müşteri satırına ekleniyor). 3 yeni backend testi (üstlenme, bırakma, kiracı izolasyonu). 124 backend testi, hepsi geçiyor. Gerçek DB'ye karşı da doğrulandı.

- **"Özgün 10 özellik" listesi #6 — Konuşma iş parçacığı (threaded follow-up):** Yeni `ticket_messages` tablosu (migration `1f76048757cf` — `ticket_id`, `sender_type` "customer"|"agent", `sender_name` (gönderim anında sabitlenir), `body`, `created_at`). **Bilinçli tasarım kararı:** bu tablo `draft_responses`'a hiç dokunmuyor — ilk AI destekli yanıt hâlâ taslak+onay akışından geçiyor, "cevaplandı" tanımı hâlâ sadece `ANSWERED_DRAFT_STATUSES`'a dayanıyor; `ticket_messages` sadece o ilk yanıttan SONRAKİ serbest metin konuşmayı tutuyor. Takip mesajları AI taslağı İÇERMİYOR — ne müşteri ne temsilci mesajı bir onay kuyruğuna düşmüyor, doğrudan gönderiliyor (İnsan onaylı akış ilkesi ilk AI yanıtı için var, burada zaten hiç AI yok, ihlal söz konusu değil). İki taraf da kendi router'ında (`tickets.py` temsilci için `require_agent`, `me.py` müşteri için `require_auth`) simetrik `GET/POST .../messages` uç noktalarına sahip — aynı `TicketMessageRead`/`TicketMessageCreate` şemalarını paylaşıyorlar. **Refactor:** `tickets.py`'de `get_ticket`/`update_ticket_assignment` arasında tekrar eden "talep gerçekten bana mı ait" kontrolü artık üçüncü/dördüncü kez tekrar etmesin diye `_get_own_ticket` yardımcısına çıkarıldı (drafts.py/kb_suggestions.py/me.py'deki aynı isimli yardımcılarla aynı desen — kasıtlı olarak router'lar arası import edilmiyor, her router kendi küçük kopyasını tutuyor). Frontend: yeni `MessageThread.tsx` — hem `/dashboard/tickets/[id]` hem `/portal/tickets/[id]`'de AYNI bileşen kullanılıyor, `role` prop'una göre hangi API fonksiyonunun (`sendTicketMessage` vs `sendMyTicketMessage`) çağrılacağına karar veriyor; kendi mesajların `accent-soft` zeminde, karşı tarafınkiler nötr zeminde ayrışıyor, her mesajın altında gönderenin adı + rolü (Temsilci/Müşteri) + zaman damgası var. 4 yeni backend testi (agent+customer için 404/kiracı-sahiplik izolasyonu ve doğru gönderen bilgisiyle oluşturma). 128 backend testi, hepsi geçiyor. Gerçek DB'ye karşı da doğrulandı.

- **"Özgün 10 özellik" listesi #7 — Çok dilli taslak üretimi:** Önceki bilinen sınır ("AI taslağı hep Türkçe") giderildi — ama YENİ bir dil tespit servisi/LLM çağrısı/DB alanı EKLENMEDİ. Sadece `draft_prompt.txt` ve `kb_suggestion_prompt.txt`'teki "Türkçe yaz" talimatı "müşterinin talebi hangi dilde yazılmışsa o dilde yaz (emin değilsen Türkçe)" ile değiştirildi — Gemini zaten tek bir talimatla dil tespiti + o dilde yazmayı aynı anda yapabiliyor, ayrı bir sınıflandırma adımına gerek yok (aynı "erken optimizasyona hayır" prensibi). **Bilinçli olarak yapılmadı:** taslağın hangi dilde yazıldığını gösteren bir UI rozeti/DB alanı — dil ayrıca saklanmıyor, agent taslağı okuyunca zaten görüyor. **Doğrulama farklı bir yöntemle yapıldı** (bu özellik mock'lanamaz, gerçek bir LLM çağrısı gerektirir): `call_llm`, yeni prompt'la, İngilizce bir müşteri talebi + bilinçli olarak TÜRKÇE bir SSS kaynağıyla gerçek Gemini API'sine karşı çalıştırıldı — model doğru şekilde İngilizce cevap yazdı VE SSS'te olmayan bir detayı (spesifik gecikme nedeni) uydurmak yerine bunu temsilciye not olarak işaretledi (mevcut "uydurma" kuralı da bozulmamış). Backend test paketi (128 test) prompt metnine bağlı olmadığı için değişmeden geçti — yeni test eklenmedi, çünkü bu davranışı sahte (mock) bir `call_llm` ile anlamlı şekilde test etmek mümkün değil.

- **"Özgün 10 özellik" listesi #8 — Güvene göre öncelikli onay kuyruğu:** `GET /tickets`'e üçüncü bir `sort` değeri: `sort=priority`. **Kesinlikle otomatik onay/gönderim DEĞİL** — sadece hangi taslağa önce bakılacağını belirleyen bir SIRALAMA. Yeni `pending_draft_confidence_clause()` (mevcut `pending_draft_id_clause` ile aynı desende bir correlated subquery) talebin en yeni pending taslağının güven skorunu döner; `list_tickets`'ın `order_by`'ı artık tek bir ifade yerine bir tuple — `priority` modunda `(Ticket.is_urgent.desc(), confidence.asc().nulls_last())`: önce acil talepler, sonra en düşük güvenli (en belirsiz) pending taslaklar, taslağı hiç olmayanlar en sona düşer. Frontend: `TicketFilterBar`'daki tek-butonlu En Yeni/En Eski toggle'ı (artık 3 seçenek gerektiği için) üç ayrı chip'e (En Yeni/En Eski/Öncelikli) dönüştürüldü — dosyadaki diğer filtre gruplarıyla (Fırsat, Aciliyet) aynı görsel dil, artık kullanılmayan özel `SortIcon` bileşeni kaldırıldı. 1 yeni backend testi (SQL'in gerçekten `is_urgent DESC` + `NULLS LAST` ürettiğini doğruluyor). 129 backend testi, hepsi geçiyor.

- **"Özgün 10 özellik" listesi #9 — AI performans trendi:** `DraftTrendPoint(DraftTotals)` şeması (miras yoluyla `approval_rate` computed field'ını bedavaya alıyor) + `AnalyticsRead.draft_trend`. Yeni `_draft_trend()` — `_daily_ticket_counts` ile birebir aynı "günlük grupla + en son 30 gün" deseni, ama `_draft_totals`'ın tüm sütunlarını (pending/approved/edited/rejected/ortalama güven/eskale) GÜN BAZINDA hesaplıyor. Frontend: yeni `DraftTrendChart.tsx` — ortalama güven skoru sabit 0-1 ölçekli bir çizgi grafiği (TicketVolumeChart'ın göreli-max ölçeği burada YANILTICI olurdu, bilinçli olarak farklı), onay oranı ise ilk/son gün karşılaştırması olarak metinle (bazı günler hiç karara bağlanmış taslak olmayabilir — payda 0, oran null, o günler atlanıp en yakın geçerli uç noktalar kullanılıyor). `/dashboard/analytics`'e eklendi. 2 yeni backend testi + 2 mevcut testin (sabit sayıda `db.execute` mock'u varsayan) side_effect listesi yeni 5. sorgu için güncellendi. 131 backend testi, hepsi geçiyor.

Sırada ve SON madde: kullanıcı 2026-08-24'te "projeye özgün, ileri taşıyacak" 10 özellik önerisini onayladı ve "sırasıyla yapalım" dedi — bkz. proje hafızası `project_10_ozgun_ozellik_onerisi` (1-9 bitti, sırada 10. madde: gerçek zamanlı bildirim — yeni lead/acil talep geldiğinde temsilciye in-app veya Slack uyarısı). Bu oturum kullanım limiti nedeniyle burada durduruldu; 55+ dosyalık değişiklik hâlâ commit edilmedi. Hafta 6'nın son fikri (CRM entegrasyonu) ve Hafta 5'in geri kalanı (eval + e-posta) hâlâ ayrıca askıda.

Roadmap ötesi ürünleşme fikirleri (analytics ekranı, temsilci düzenleme farkının kaydı, multi-tenant kararı) konuşuldu ama henüz uygulanmadı — bkz. proje hafızası `project_urunlesme_fikirleri`.

İlerledikçe bu bölümü güncel aşamayı yansıtacak şekilde elle güncelle.

## Teknoloji Stack'i

- **Backend:** Python + FastAPI
- **Frontend:** Next.js (React, App Router)
- **Veritabanı:** PostgreSQL + pgvector eklentisi (hem ilişkisel veri hem vektör arama aynı veritabanında)
- **LLM:** Google Gemini API
- **Auth:** Clerk (eklendi, bkz. "Mevcut Aşama")
- **E-posta:** Gerçek gelen e-posta girişi denendi (SendGrid, Postmark, Mailgun) ama domain şartı yüzünden şimdilik bırakıldı — bkz. "Mevcut Aşama". Talep girişi şu an form + müşteri portalı üzerinden.
- **Deployment:** Railway (backend + DB), Vercel (frontend) — henüz eklenmedi

## Mimari Prensipler

- **İnsan onaylı akış:** Her AI taslağı bir onay kuyruğuna düşer; otomatik gönderim yoktur.
- **Modülerlik:** Sınıflandırma, RAG/retrieval, taslak üretimi ve güven skoru hesaplama birbirinden bağımsız fonksiyonlar/modüller olarak yazılır, tek bir dev fonksiyona sıkıştırılmaz.
- **İnce soyutlama, ağır framework değil:** LLM çağrıları için `call_llm(prompt, context)` gibi basit, kendi yazdığımız bir fonksiyon kullanılır. Bu, hem mimariyi anlamayı kolaylaştırır hem de sağlayıcı değişikliğini tek noktadan yönetmeyi sağlar.
- **İzlenebilirlik:** Her taslak, hangi bilgi tabanı parçalarına (`retrieved_context`) dayandığını kaydeder. Bu alan asla atlanmamalı — hata ayıklama ve eval için kritik.
- **Erken optimizasyona hayır:** Özel vektör veritabanı, karmaşık agent framework'leri, fine-tuning gibi adımlar MVP'de yok; sadece gerçek ihtiyaç ortaya çıkınca eklenir.

## Klasör Yapısı (önerilen)

```
/backend
  /app
    /routers        # FastAPI route'ları (tickets, drafts, auth vb.)
    /services        # classification.py, retrieval.py, draft_generation.py, confidence.py
    /models          # SQLAlchemy modelleri
    /db              # bağlantı, migration'lar
  main.py
/frontend
  /app               # Next.js sayfaları (dashboard, ticket detay vb.)
  /components
/scripts
  seed_tickets.py     # örnek talep verisini yükler
  ingest_kb.py         # SSS/dokümantasyonu embed edip pgvector'a yazar
CLAUDE.md
akilli-destek-asistani-proje-raporu.md
```

## Veri Modeli (özet — tam şema rapor dosyasında)

`tickets`, `knowledge_base_chunks` (embedding sütunu ile), `draft_responses` (`retrieved_context`, `confidence_score`, `status` alanlarıyla), `agents`, `eval_examples`.

## Kodlama Standartları

- Backend: tip belirteçleri (type hints) kullan, Pydantic modelleriyle giriş/çıkış doğrulaması yap.
- Her yeni servis fonksiyonu (classification, retrieval, draft generation) için en az birkaç birim test yaz.
- LLM prompt'larını kod içine gömmek yerine ayrı `.txt`/`.py` sabitleri olarak tut, böylece değişiklik geçmişi net görünür.
- Gizli anahtarlar (`.env`) asla commit edilmez; `.env.example` ile hangi değişkenlerin gerektiği belgelenir.

## Yol Haritası (özet)

1. Hafta: İskelet — backend/frontend kurulumu, `tickets` tablosu, örnek veri, listeleme ekranı.
2. Hafta: Bilgi tabanı + RAG — pgvector kurulumu, SSS içeriği, embedding + retrieval fonksiyonu.
3. Hafta: Sınıflandırma + taslak üretimi — LLM entegrasyonu, dashboard'da taslak gösterimi.
4. Hafta: Güven skoru + eskalasyon — eşik mantığı, onay/düzenleme/red kaydı, eval seti.
5. Hafta: Gerçek entegrasyon — e-posta, auth, temel analitik.
6. Hafta ve sonrası: Genişletmeler (lead qualification, CRM entegrasyonu, tool-use).

Tam gerekçeler ve detaylar için `akilli-destek-asistani-proje-raporu.md` dosyasına bakılmalı.

## Kesinlikle Yapılmaması Gerekenler

- AI'ın taslağı insan onayı olmadan doğrudan müşteriye göndermesi.
- Gerçek müşteri verisiyle, önce eval seti üzerinde test edilmeden yeni bir prompt/model değişikliği yapmak.
- `.env` dosyasını veya API anahtarlarını commit etmek.
- Bu aşamada (Hafta 1-4) LangChain/LangGraph gibi ağır framework'ler eklemek — Bölüm "Mimari Prensipler"e bakılmalı.
